# macOS
.DS_Store

# Xcode user data
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/
DerivedData/
build/
.build/

# The Xcode project can be regenerated from iosApp/project.yml via `xcodegen`.
# It is kept committed so the app opens in Xcode without extra tooling.

# CocoaPods / SPM (not used, kept for safety)
Pods/
Podfile.lock
.swiftpm/

# Misc
*.moved-aside
*.hmap
*.ipa
fastlane/.env
fastlane/*.p8
vendor/bundle/
.bundle/
